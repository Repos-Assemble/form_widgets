# form_widgets

A collection of form components, part of the Repos Assemble package suite.

This package is currently a starting skeleton — add your form widgets under `lib/src/` and export them from `lib/form_widgets.dart`.
